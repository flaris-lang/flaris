# Flaris benchmark results

**Date:** 2026-08-21
**Machine:** Apple M5, macOS 26.5.1 (arm64)
**Flaris VM:** 1.0.1.0

Generated by `bench/run_bench.sh`. Every number is the median of 3 runs,
measured inside the program, so process startup is excluded. Lower is better.
Interpretation of these numbers lives in [../README.md](../README.md).

## Toolchain

| Language | Version | Mode |
| -------- | ------- | ---- |
| C | Apple clang version 21.0.0 (clang-2100.1.1.101) | -O2 |
| Go | go version go1.27.0 darwin/arm64 | go build |
| Nim | Nim Compiler Version 2.2.10 [MacOSX: arm64] | -d:release --opt:speed -O3 |
| Python | Python 3.9.6 | CPython interpreted |
| Lua | Lua 5.5.1  Copyright (C) 1994-2026 Lua.org, PUC-Rio | interpreted |
| LuaJIT | LuaJIT 2.1.1787165859 -- Copyright (C) 2005-2026 Mike Pall. https://luajit.org/ | tracing JIT |
| Node | v26.7.0 | V8 |
| QuickJS | QuickJS version 2026-06-04 | interpreted |
| Flaris | 1.0.1.0 | bytecode VM (`--strip`) |

## Integer kernels

Pure arithmetic and control flow, no library calls. These measure the VM's
dispatch loop and JIT backend.

### 1. Recursive Fibonacci - `fib(38)` = 39,088,169

Roughly 126 M recursive calls. Measures function-call overhead and branch
prediction.

| Language | time (ms) | vs C | peak RSS (MB) |
| -------- | --------: | ----: | ------------: |
| C (clang -O2) | 60 | 1.0x | 1.3 |
| Go (go build) | 90 | 1.5x | 4.0 |
| LuaJIT 2.1 (tracing JIT) | 168 | 2.8x | 1.9 |
| Nim (-d:release -O3) | 176 | 2.9x | 1.4 |
| Flaris JIT (bytecode VM + JIT) | 193 | 3.2x | 7.0 |
| Lua 5.4 (interpreted) | 1152 | 19.2x | 1.7 |
| Flaris (bytecode VM, JIT off) | 2073 | 34.5x | 6.9 |
| QuickJS (interpreted) | 2785 | 46.4x | 2.0 |
| Python 3 (CPython) | 5702 | 95.0x | 8.2 |

### 2. Sieve of Eratosthenes - `N = 10,000,000`, 664,579 primes

Byte-array write-heavy inner loop. Tests memory bandwidth and array indexing
overhead. Each language uses its typed byte buffer (C `uint8_t*`, Nim
`seq[uint8]`, Python `bytearray`, JS `Uint8Array`, Lua table, Flaris `Buffer`).

| Language | time (ms) | vs C | peak RSS (MB) |
| -------- | --------: | ----: | ------------: |
| C (clang -O2) | 16 | 1.0x | 10.9 |
| Go (go build) | 29 | 1.8x | 14.2 |
| Nim (-d:release -O3) | 30 | 1.9x | 10.9 |
| Flaris JIT (bytecode VM + JIT) | 31 | 1.9x | 16.6 |
| LuaJIT 2.1 (tracing JIT) | 76 | 4.8x | 129.9 |
| Lua 5.4 (interpreted) | 211 | 13.2x | 238.3 |
| Flaris (bytecode VM, JIT off) | 266 | 16.6x | 16.5 |
| QuickJS (interpreted) | 410 | 25.6x | 11.7 |
| Python 3 (CPython) | 756 | 47.2x | 17.8 |

### 3. Collatz total steps - `N = 1,000,000`

Sum of Collatz sequence lengths for n = 1…1 M, where each step is
`n = odd ? 3n+1 : n/2`. Data-dependent branching that cannot be vectorised.

| Language | time (ms) | vs C | peak RSS (MB) |
| -------- | --------: | ----: | ------------: |
| C (clang -O2) | 103 | 1.0x | 1.3 |
| Go (go build) | 156 | 1.5x | 4.0 |
| Nim (-d:release -O3) | 181 | 1.8x | 1.4 |
| Flaris JIT (bytecode VM + JIT) | 493 | 4.8x | 7.0 |
| LuaJIT 2.1 (tracing JIT) | 694 | 6.7x | 1.8 |
| Lua 5.4 (interpreted) | 2055 | 20.0x | 1.6 |
| Flaris (bytecode VM, JIT off) | 2392 | 23.2x | 6.8 |
| QuickJS (interpreted) | 3858 | 37.5x | 2.0 |
| Python 3 (CPython) | 9182 | 89.1x | 8.2 |

### Kernel summary

| Language | fib(38) | sieve(10M) | collatz(1M) | avg ×C |
| -------- | ------: | ---------: | ----------: | -----: |
| C | 60 ms | 16 ms | 103 ms | 1.0x |
| Go | 90 ms | 29 ms | 156 ms | 1.6x |
| Nim | 176 ms | 30 ms | 181 ms | 2.2x |
| Flaris JIT | 193 ms | 31 ms | 493 ms | 3.3x |
| LuaJIT 2.1 | 168 ms | 76 ms | 694 ms | 4.8x |
| Lua 5.4 | 1152 ms | 211 ms | 2055 ms | 17.5x |
| Flaris | 2073 ms | 266 ms | 2392 ms | 24.8x |
| QuickJS | 2785 ms | 410 ms | 3858 ms | 36.5x |
| Python 3 | 5702 ms | 756 ms | 9182 ms | 77.1x |

## Real-world builtins

These exercise the library paths users actually reach for - `Json.Parse`,
string concatenation and `Regex.Matches` - rather than pure integer kernels.
Every language reads identical generated input and returns the same result.

### 1. JSON parse - 18 MB array, 300k records, 5 iterations

Parse the same 18 MB document five times, summing every record's integer field.
Measures a JSON parser plus object/field materialisation. `result = 749,250,000`.

| Language | time (ms) | vs fastest | peak RSS (MB) |
| -------- | --------: | ----: | ------------: |
| Node/V8 (V8) | 324 | 1.0x **fastest** | 214.2 |
| Flaris JIT (bytecode VM + JIT) | 571 | 1.8x | 208.3 |
| Flaris (bytecode VM, JIT off) | 585 | 1.8x | 208.2 |
| Go (go build) | 664 | 2.0x | 102.3 |
| Python 3 (CPython) | 1049 | 3.2x | 260.0 |
| QuickJS (interpreted) | 1165 | 3.6x | 164.6 |
| Nim (-d:release -O3) | 1546 | 4.8x | 336.4 |

### 2. String build - naive `s += piece`, N = 1,000,000

Grow a string one small piece at a time - the obvious, naive loop.
`result = 11,888,890` bytes. Go uses `strings.Builder`, Lua/LuaJIT use
`table.concat` and Nim uses `s.add`, because naive concatenation is quadratic
there; Python, Node, QuickJS and Flaris use the naive `s += piece` form.

| Language | time (ms) | vs fastest | peak RSS (MB) |
| -------- | --------: | ----: | ------------: |
| Go (go build) | 40 | 1.0x **fastest** | 31.8 |
| Nim (-d:release -O3) | 40 | 1.0x **fastest** | 35.9 |
| Flaris JIT (bytecode VM + JIT) | 102 | 2.5x | 20.3 |
| Node/V8 (V8) | 103 | 2.6x | 149.2 |
| Flaris (bytecode VM, JIT off) | 113 | 2.8x | 20.2 |
| Python 3 (CPython) | 223 | 5.6x | 21.7 |
| LuaJIT 2.1 (tracing JIT) | 245 | 6.1x | 86.5 |
| QuickJS (interpreted) | 415 | 10.4x | 19.0 |
| Lua 5.4 (interpreted) | 510 | 12.8x | 106.5 |

### 3. Regex match - 9.8 MB corpus, 5 scans

Count every `\b\d{4}-\d{2}-\d{2}\b` date across a log-like corpus in which two
of every three lines are near-miss decoys. `result = 500,000`.

| Language | time (ms) | vs fastest | peak RSS (MB) |
| -------- | --------: | ----: | ------------: |
| Node/V8 (V8) | 154 | 1.0x **fastest** | 112.6 |
| Flaris JIT (bytecode VM + JIT) | 434 | 2.8x | 33.7 |
| Flaris (bytecode VM, JIT off) | 456 | 3.0x | 33.8 |
| QuickJS (interpreted) | 689 | 4.5x | 33.3 |
| Nim (-d:release -O3) | 821 | 5.3x | 17.7 |
| Python 3 (CPython) | 897 | 5.8x | 35.2 |
| Go (go build) | 1429 | 9.3x | 35.1 |

## Methodology

- Times are wall-clock, measured inside the program with the language's own
  monotonic clock, so interpreter startup and input reading are never counted.
- Each figure is the median of several runs (`RUNS`, default 3).
- Every language must produce the same `result:` integer for a benchmark; the
  runner cross-checks them and warns on any disagreement.
- Flaris runs with `--strip` so debug metadata is not loaded, and appears twice:
  the plain bytecode VM (`--jit-disable`), and the default build with the native
  JIT backend enabled.
- Lua reports `os.clock()` (CPU time); everything else uses a wall clock. On an
  idle machine these agree for single-threaded work.
- Runtimes that are not installed are skipped and omitted from the tables
  rather than reported as zero.

### Reading the peak RSS column

Peak resident set size is measured from outside the process with `time(1)`, so
unlike the timings it **includes the runtime's own baseline** - interpreter,
JIT code cache, garbage-collector heap high-water mark and allocator arenas.
Two consequences worth keeping in mind:

- On the small integer kernels the figure is dominated by startup footprint,
  not by the benchmark. `fib` allocates essentially nothing, so that column is
  really "what does this runtime cost to have running at all".
- Runtimes with a tracing collector are measured at their heap high-water mark,
  which is a scheduling artefact as much as a workload property: a collector
  that runs less often looks worse here without being less efficient. Flaris
  uses reference counting over pre-allocated SLAB pools and has no tracing
  collector, so its figure tracks live data more directly than Go's or Node's.

The memory numbers are therefore best read within a row-group of similar
runtimes, and on the workloads that actually build a large object graph - the
JSON benchmark most of all - rather than as a single cross-language ranking.
