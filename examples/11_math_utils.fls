// 11_math_utils.fls
// A tiny reusable module

class MathUtils
{
    fn static square(x: int): int
    {
        return x * x;
    }

    fn static isEven(x: int): bool
    {
        return (x % 2) == 0;
    }
}

// Explicit exports
export {
    MathUtils
};
